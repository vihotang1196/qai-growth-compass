node_modules
dist
dist-ssr
.vercel
*.local
*.tsbuildinfo

# 环境变量:一律不进仓库
.env
.env.*
!.env.example

# 原始字体源文件(单个约 10MB,只用于本地 subset 生成,不进 git)
# 见 PROGRESS.md 0.14
assets/fonts/

# 生成产物
public/fonts/*.subset.woff2

.DS_Store
*.log
.idea
.vscode/*
!.vscode/extensions.json

# 本地 npm 缓存(~/.npm 权限受限时使用)
.npm-cache
